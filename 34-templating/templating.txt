EJS -> Embedded JavaScript

npm i ejs -> installing ejs in bash after app.set('view engine', 'ejs')

res.render('home.ejs') is used for templating
and seeing the content of html

                     ***ejs tags***
<% 'Scriptlet' tag, for control-flow, no output                                                 -> multiple lines
<%_ ‘Whitespace Slurping’ Scriptlet tag, strips all whitespace before it
<%= Outputs the value into the template (HTML escaped)  // treated as js computation in html    -> one line
<%- Outputs the unescaped value into the template
<%# Comment tag, no execution, no output
<%% Outputs a literal '<%'
%> Plain ending tag
-%> Trim-mode ('newline slurp') tag, trims following newline
_%> ‘Whitespace Slurping’ ending tag, removes all whitespace after it


There was a problem with bootstrap in the section of templating, before the last video